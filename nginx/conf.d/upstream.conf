# =============================================================================
# Upstream Servers Configuration
# Can be included separately for easy modification
# =============================================================================

# Frontend Server (React/Vite Development)
upstream frontend_server {
    server 127.0.0.1:5000 weight=1 max_fails=3 fail_timeout=30s;
    
    keepalive 32;
    keepalive_timeout 60s;
    keepalive_requests 100;
}

# Backend Server (FastAPI/Python)
upstream backend_server {
    server 127.0.0.1:8000 weight=1 max_fails=3 fail_timeout=30s;
    
    keepalive 32;
    keepalive_timeout 60s;
    keepalive_requests 100;
}

# Load Balanced Backend (for multiple instances)
# upstream backend_lb {
#     least_conn;
#     
#     server 127.0.0.1:8000 weight=1;
#     server 127.0.0.1:8001 weight=1;
#     server 127.0.0.1:8002 weight=1;
#     
#     keepalive 64;
# }
